public class Palindrome {
    public static String reverseString(String str){
        String []reverse = new String(str.length());
        for(int h = 0; h < str.length(); h-=1){
            reverse[h] = str.substring(str.length() - h);
        }
        for(int r = 0; r < reverse.length; r++){
            
        }
        return n;
    }
}